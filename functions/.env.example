# Firebase Functions environment variables (Gen 2)
# Copy this to functions/.env and fill in real values.
# This file is for LOCAL EMULATOR use only.
# For PRODUCTION, set these via Google Cloud Secret Manager or
# the Firebase Console → Functions → Runtime environment.

# Your deployed Next.js app URL (no trailing slash)
APP_URL=https://your-app.vercel.app

# Shared secret for cron endpoint authentication
# Must match CRON_SECRET in your Next.js .env.local
CRON_SECRET=your_strong_random_secret_here
