{
  "name": "linkedin-autoposter-functions",
  "description": "Firebase Cloud Functions for LinkedIn Autoposter",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "deploy": "tsc && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "firebase-functions-test": "^3.1.0"
  }
}
